import { Inter, Lusitana, DynaPuff } from 'next/font/google';

 
export const inter = Inter({ subsets: ['latin'] });
export const lusitana = Lusitana({ subsets: ['latin'], weight: '400' });
export const dynaPuff = DynaPuff({ subsets: ['latin'] });