// Keep font usage build-safe by relying on Tailwind font-family classes.
// Dev can load Google Fonts through the root layout; prod falls back cleanly.
export const inter = { className: 'font-inter' };
export const dynaPuff = { className: 'font-dynapuff' };
export const montserrat = { className: 'font-montserrat' };
